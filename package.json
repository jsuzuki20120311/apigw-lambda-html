{
  "name": "apigw-lambda-html",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf ./dist && mkdir -p ./dist && zip -r ./dist/build.zip ./ -x \"dist/*\""
  },
  "keywords": ["lambda", "aws"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ejs": "~3.1.9"
  }
}
